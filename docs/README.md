# Docs Index

- Read `mvp-scope.md` only when changing features, browser target, permission scope, or the main export flow.
- Read `export-format.md` only when changing saved files, JSON/Markdown shape, attachment handling, packaging, or filenames.
- Read `tech-stack.md` only when changing extension architecture, runtime contexts, build tooling, permissions implementation, or cross-browser strategy.
- Read `ui-ux.md` only when changing popup/page layout, states, labels, or interaction flow.
- Do not read all docs by default.
