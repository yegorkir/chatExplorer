# Docs Index

- Read `mvp-scope.md` only when the task changes MVP boundaries, UX flow, or feature scope.
- Read `export-format.md` only when the task changes exported files, JSON shape, attachment handling, packaging, or filenames.
- Read `tech-stack.md` only when the task changes tooling, architecture, rendering model, worker usage, or dependency direction.
- Do not read all docs by default.

- `ui-ux.md` - read only for page structure, user flow, states, labels, and interaction behavior
